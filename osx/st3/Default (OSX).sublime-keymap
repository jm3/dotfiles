[
  { "keys": ["super+shift+s"], "command": "abacus" },

  // this plugin just filters the selection, and can cull lines
  // it does NOT filter lines through an external command. doh!
  { "keys": ["super+l"], "command": "filter_to_lines", "args": { "search_type": "regex" } },
  { "keys": ["super+k", "super+s"], "command": "filter_to_lines", "args": { "search_type": "string" } },

]
